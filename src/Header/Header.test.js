import React from 'react';
import { render } from '@testing-library/react';

describe('<Header/>', () => {
	describe('desktop view', () => {
		describe('on render', () => {
			it('should render', () => {

			})

			it('should render text logo', () => {

			})

			it('should render nav links', () => {

			})
		})
		
		describe('nav links', () => {
			describe('on hover', () => {

			})
	 
			describe('on click', () => {

			})
		}) 
	})

	describe('mobile view', () => {
		describe('on render', () => {
			it('should render', () => {

			})

			it('should render text logo', () => {

			})

			it('should render burger', () => {

			})
		}) 

		describe('on click burger', () => {
			it('should add ".burger-selected" to burger', () => {

			})
		})
	})
})
